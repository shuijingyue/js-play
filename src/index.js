// for (let i = 1; i <= 1080; i++) {
//   console.log(`<dimen name="px_${i}">${(i / 2.75).toFixed(2)}dp</dimen>`);
// }
for (let i = 1; i <= 54; i++) {
  console.log(`<dimen name="px_${i}">${(i / 3).toFixed(2)}dp</dimen>`);
}